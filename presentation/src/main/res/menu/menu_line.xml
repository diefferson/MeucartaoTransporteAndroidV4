<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="br.com.disapps.meucartaotransporte.activity.CartoesActivity">

    <item android:id="@+id/fav_linha"
        android:icon="@drawable/star_outline"
        android:title="Favoritar Linha"
        app:theme="@style/ThemeOverlay.AppCompat.ActionBar"
        app:showAsAction="ifRoom"
        android:orderInCategory="1"
        android:menuCategory="secondary"/>

    <item android:id="@+id/shape_linha"
        android:icon="@drawable/map"
        android:title="Ver Trajeto"
        app:theme="@style/ThemeOverlay.AppCompat.ActionBar"
        app:showAsAction="ifRoom"
        android:orderInCategory="1"
        android:menuCategory="secondary"/>

    <item android:id="@+id/pontos_linha"
        android:icon="@drawable/map_marker"
        android:title="Ver Pontos"
        app:theme="@style/ThemeOverlay.AppCompat.ActionBar"
        app:showAsAction="ifRoom"
        android:orderInCategory="1"
        android:menuCategory="secondary"/>

    <item android:id="@+id/veiculos_linha"
        android:icon="@drawable/bus"
        android:title="Ver Veículos"
        app:theme="@style/ThemeOverlay.AppCompat.ActionBar"
        app:showAsAction="ifRoom"
        android:orderInCategory="1"
        android:menuCategory="secondary"/>

</menu>
