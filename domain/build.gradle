apply plugin: 'kotlin'

kotlin {
    experimental {
        coroutines "enable"
    }
}

dependencies {

    rootProject.domainDependencies.each {
        add(it.configuration, it.dependency, it.options)
    }
}